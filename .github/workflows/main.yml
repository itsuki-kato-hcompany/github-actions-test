# アクション名
name: Github Action Test

# タイミングを指定
on:
  push:
    branches: ["master"]
  pull_request:
    branches: ["master"]
    
jobs:
  Test:
    name: Run test codes
    runs-on: ubuntu-latest
    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Test
        run: echo Hello World
